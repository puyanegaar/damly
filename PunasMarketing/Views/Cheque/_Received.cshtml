@model int?

<div class="modal-header bg-danger">
    <button type="button" class="close" data-dismiss="modal">
        <span aria-hidden="true" style="color: white">×</span>
        <span class="sr-only">Close</span>
    </button>
    <h5 class="modal-title">تغییر وضعیت چک به دریافتی</h5>
</div>

<div id="myDiv">
    @using (Html.BeginForm("ReceivedModal", "Cheque", FormMethod.Post, new { style = "padding: 10px" }))
    {
        @Html.HiddenFor(model => model.Value)

        <h4>آیا می خواهید وضعیت چک را به دریافتی تغییر دهید؟</h4>
        <h5>توجه: سند وصول چک یا سند واگذاری چک به بانک یا سند عودت، در صورت وجود حذف خواهند شد و وضعیت چک به حالت عادی بر می گردد.</h5>

        <input class="btn btn-outline-success btn-next" type="submit" value="تأیید" />
        <input class="btn btn-outline-danger btn-next" type="button" value="لغو" data-dismiss="modal" />
    }

</div>